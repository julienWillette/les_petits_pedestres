
{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}

<div class="container-fluid">
<div class="col-md-3">
{{ form_start(form) }}
  {{ form_row(form.q) }}
<h4>Catégories</h4>
  {{ form_row(form.category) }}
<h4>Taille</h4>
  {{ form_row(form.size) }}
  <h4>Prix</h4>
  <div class="row">
    <div class="col-md-6">
      {{ form_row(form.min) }}
    </div>
    <div class="col-md-6">
      {{ form_row(form.max) }}
    </div>
  </div>
  <div id="price-slider" data-min="{{ min }}" data-max="{{ max }}" class="mt-3 mb-3"></div>

  <h4>Promotions</h4>
  {{ form_row(form.promo) }}

<button type="submit" class="btn btn-primary w-100">Filtrer</button>

{{ form_end(form) }}
<div class="col-md-9">


 {% for product in products %}
     {{dump(product.name)}}
 {% endfor %}

</div>
</div>


{% endblock %}
